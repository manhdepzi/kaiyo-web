<!DOCTYPE html><html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>@yield('title','Admin — Kaiyo')</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="min-h-screen bg-canvas text-ink antialiased"><a href="#admin-content" class="sr-only focus:not-sr-only">Đi tới nội dung chính</a><header class="border-b border-line bg-surface"><div class="mx-auto flex max-w-[1440px] items-center justify-between px-5 py-4 lg:px-8"><span><a href="{{ route('home') }}" class="font-bold tracking-[0.18em] text-brand">KAIYO</a><small class="ml-4 text-ink-muted">Admin Workspace</small></span><nav class="flex gap-2">@if($adminNavigation['content'] ?? false)<x-ui.button :href="route('admin.content')" variant="secondary" size="sm">Nội dung</x-ui.button>@endif @if($adminNavigation['merchant'] ?? false)<x-ui.button :href="route('admin.merchant')" variant="secondary" size="sm">Merchant</x-ui.button>@endif @if($adminNavigation['analytics'] ?? false)<x-ui.button :href="route('admin.analytics')" variant="secondary" size="sm">Analytics</x-ui.button>@endif @if($adminNavigation['outbox'] ?? false)<x-ui.button :href="route('admin.outbox')" variant="secondary" size="sm">Outbox</x-ui.button>@endif @if($adminNavigation['audit'] ?? false)<x-ui.button :href="route('admin.audit')" variant="secondary" size="sm">Audit</x-ui.button>@endif <x-ui.button :href="route('account.security')" variant="ghost" size="sm">Bảo mật</x-ui.button></nav></div></header><main id="admin-content">@yield('content')</main></body></html>
